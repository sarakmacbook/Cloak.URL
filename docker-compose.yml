version: "3.8"

services:
  shorten:
    build: .
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - BASE_URL=http://localhost:3000
      - DB_PATH=/app/data/urls.db
      - MAX_LINKS=10000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
